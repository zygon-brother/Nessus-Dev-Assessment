#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wait-for-port/bin/wait-for-port" "$@"
  ret=$?
else 
  node  "$basedir/../wait-for-port/bin/wait-for-port" "$@"
  ret=$?
fi
exit $ret
