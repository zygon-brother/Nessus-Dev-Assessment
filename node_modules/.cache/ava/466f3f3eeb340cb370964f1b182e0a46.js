'use strict';

const test = require('ava');
const request = require('supertest');
const server = require('../server');

test.beforeEach(t => {
	server().start();
});

test('A first unit test', t => {
	const res = request('http://localhost:8080').get('/hosts').set('Accept', 'application/json');

	console.log('HELLO!  ' + JSON.stringify(res));
	t.is(res.status, 200);
	//t.is
});
//# sourceMappingURL=..\node_modules\.cache\ava\466f3f3eeb340cb370964f1b182e0a46.js.map