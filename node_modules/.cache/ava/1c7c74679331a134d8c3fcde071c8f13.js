'use strict';

const test = require('ava');
const request = require('supertest');
const server = require('../server');
const controller = require('../controllers');

test.beforeEach(t => {
	server().start();
});

test('A first unit test', t => {
	controller.setTestVals({
		"id": 1,
		"name": "localhost.localdomain",
		"ipv4": "127.0.0.1"
	});

	const res = request('http://localhost:8080').get('/hosts');
	console.out("Hello! " + res.status);
	t.is(res.status, 200);
});
//# sourceMappingURL=..\node_modules\.cache\ava\1c7c74679331a134d8c3fcde071c8f13.js.map