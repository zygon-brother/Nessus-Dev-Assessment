'use strict';

const test = require('ava');
const request = require('supertest');
const server = require('../server');

test.beforeEach(t => {
	server().start();
});

test('A first unit test', t => {
	const res = request('http://localhost:8080').get('/hosts').set('Accept', 'application/json').expect(200).end(function (err, res) {
		if (err) return done(err);
		done();
	});

	t.is(res.status, 200);
	t.is;
});
//# sourceMappingURL=..\node_modules\.cache\ava\78b6faf674c6ff5e8e42611e943d469c.js.map