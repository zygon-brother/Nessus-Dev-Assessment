'use strict';

const test = require('ava');
const request = require('supertest');
const server = require('../server');

test.beforeEach(t => {
	server().start();
});

test('A first unit test', t => {
	const res = request('http://localhost:8080').get('/hosts').set('Accept', 'application/json').expect(404).end(function (err, res) {
		if (err) return done(err);
		done();
	});

	t.is(res.status, 200);
	//t.is
});
//# sourceMappingURL=..\node_modules\.cache\ava\e22202f83b60c395e24c216ec8edb872.js.map